# Jade Pack Report

Use this template for human-readable summaries from the jade pack.

---

## Executive Summary

- `top line finding`:
- `main audience signal`:
- `main product signal`:
- `main risk`:

## Evidence Summary

- `core jade sample count`:
- `adjacent-category sample count`:
- `keyword discovery coverage`:
- `strongest recurring comments`:
- `most repeated visual cues`:

## Recommendation

- `build now`:
- `test capsule`:
- `monitor`:
- `avoid for now`:
