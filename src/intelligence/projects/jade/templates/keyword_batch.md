# Jade Keyword Batch Template

Use this template when collecting one jade keyword group at a time.

---

## Batch Metadata

- `keyword group`:
- `seed keywords`:
- `search date`:
- `source mix`:
- `review window`:

## Capture Notes

- `why this batch matters`:
- `what visual patterns showed up`:
- `what comment patterns showed up`:
- `what to re-run with close variants`:

## Output Fields

- `keyword`:
- `note id`:
- `account`:
- `interaction signal`:
- `image verdict`:
- `analysis note`:
- `next action`:
