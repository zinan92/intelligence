# Product Priority Template

Use this template to turn jade trend judgments into product action.

---

## Build Now

### Direction

- `trend cluster`:
- `target audience`:
- `product type`:
- `material mix`:
- `why now`:
- `commercial reason`:
- `content reason`:

## Test Capsule

### Direction

- `trend cluster`:
- `target audience`:
- `product type`:
- `material mix`:
- `what to test`:
- `success signal`:
- `risk`:

## Monitor

### Direction

- `trend cluster`:
- `why monitor instead of build`:
- `what evidence would upgrade it`:

## Avoid for Now

### Direction

- `trend cluster`:
- `why low-fit`:
- `main mismatch`: audience | price_band | style_language | weak_proof

